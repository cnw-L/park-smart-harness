# 项目协作约定

## 流程图默认规范

本项目后续流程图默认以 `artifacts/park-smart-harness-workflow-optimized.png` 为用户确认的视觉与信息架构基准。除非用户明确要求其他风格，否则遵循以下约定：

- 使用纵向端到端主链，按“入口 → 上下文 → 事务内圈 → 工具域 → 治理分叉 → 结果汇流 → 返回”组织。
- 保持实现级信息密度，展示真实模块、关键状态、治理判定、数据源、持久化和审计信息；不退化为只有少量业务步骤的极简图。
- 复杂度集中在 `DENY / ALLOW / ASK` 治理分叉，分支完成各自义务后必须明确接回统一结果汇流。
- 颜色语义固定：蓝色表示编排、身份和返回；绿色表示上下文、只读执行和可信事实；橙色表示控制、挂起和人工确认；灰色表示拒绝与运行元数据。
- 使用浅色背景、大标题、清晰中文字级、统一圆角卡片和实线箭头；避免深色仪表盘、微小文字、装饰性英文、交叉轨迹和未闭合连线。
- 内容与代码一致，优先使用项目中的真实名称，例如 `Principal`、`ToolLoader`、`CatalogGate`、`ConversationStore`、`ASSEMBLE → MODEL → GATE → ACT → VERIFY → COMMIT`。
- 输出 PNG 时同时保留可复现的渲染脚本；原图需要优化时保留原文件，另存优化版。
- 交付前必须进行两次检查：缩略图检查主链和分叉是否能读懂；原尺寸检查文字溢出、对齐、箭头端点和分支汇流。

用户明确要求“完全按照这个优化”或“以后流程图都这样形成”时，以上规范优先于自由风格探索。
