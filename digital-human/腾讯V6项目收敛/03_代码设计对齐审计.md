# 代码 ⇄ 设计对齐审计

> 用途：研究收敛。回答 `00 §1.5` 提出的疑点——旧代码（`smart_park_assistant/src/`）的理念是否与 `harness-park-design` 定稿设计跑偏。
> 方法：逐模块读代码，对照定稿文档（`内圈循环-收敛定稿.md` / `工具管理子系统-设计.md` / `上下文管理子系统-设计.md`）。
> 日期 2026-06-22。

---

## 0. 总结论（出乎意料地好）

> **理念没跑偏，反而对齐度很高。** 旧代码不仅符合定稿，**还已经实现到定稿「下一步」清单的大部分**——`内圈循环-收敛定稿 §七` 列的待修缺口（空文本误判、缺 stall、缺 awaiting_confirmation、墙钟预算）在现读代码里**都已修掉**。说明代码写在设计收敛之后、或同步更新过。
>
> 真正的「未完成」不是理念漂移，而是 **接缝真化**：引擎默认挂桩件（Fake 后端 / Null 校验），真实业务后端与执行后校验规则尚未接。这是预期内的分期，不是债。

---

## 1. 逐模块对齐表

| 模块 | 定稿要求 | 代码实现 | 对齐 |
|---|---|---|---|
| **内圈循环** `loop.py` | 步进式、推进到退出原因、迭代即事务、中断=不提交、挂起=pending 边界、恢复=解析整批 | `run_loop` 事务型：`_safe_commit` 按迭代提交 Boundary；中断 `del messages[committed_len:]` 不提交；`awaiting_confirmation` 提交 pending_batch；恢复解析整批替换占位 | 🟢 完全对齐 |
| **plan** `plan.py` | 砍条件 DAG，plan=全量覆盖式 todo 快照，并串靠循环动态，spec 不含 depends_on | `PlanItem(id/content/status/result/spec)`，注释明写「并串关系不进 plan…spec 不含 depends_on」；`derive_plan` 取最近全量快照 | 🟢 **疑点解除**：无 DAG 残留 |
| **闸** `gate.py`(引擎) + `agent_tools/gate.py`(真) | allow/ask/deny；deny←permission，ask←is_control | 引擎默认 `DefaultGate`(桩，denied=None)；真 `CatalogGate` 读 `spec.permission ∩ principal.permissions`→deny，is_control→ask，组合根注入 | 🟢 对齐（真件存在） |
| **控制** `control.py`(桩) + `proposal_control.py`(真) | 提议-执行分离、幂等键冻结、读回对账、is_control 不内联 | `FakeControlCapability` 桩模拟 idem 账本；真 `ProposalControlCapability` 接 backend；`execute_proposal` 走 ask→freeze→resolve | 🟢 逻辑对齐 |
| **上下文** `assembler.py` | 功能标签分层(非缓存)、丢弃按文本答案交付、裁剪按对话轮数保头尾、压缩=摘要 v2 不实现 | `ParkContextAssembler`：系统头(固定+记忆)/消息流(丢已答+排plan+裁轮数)/尾部(plan)；context awareness 余量回喂；压缩 v2 未调 | 🟢 完全对齐 |
| **工具组织** `catalog.py` + `composition.py` | 超域图 7 顶层、ToolSpec 治理元数据、is_control 注册期不变量、代码共置登记 | `ToolCatalog`+`ToolSpec`(group/risk/permission/keywords/output_budget)；`__post_init__` 校验 risk⟺is_control；组合根登记 7 工具 | 🟢 完全对齐 |
| **持久化** | Redis 热 + PG 审计/幂等账本、消息即状态、按迭代原子提交 | `redis_store.py` + `pg_store.py` + `audited_store.py`；Boundary 提交 | 🟢 对齐 |
| **verify** `verify.py` | 执行后校验在子系统、business_ok→is_error 回灌触发重规划 | 接缝 + `NullVerifier`(默认放行)；**真 validation_policy 未接** | 🟡 接缝对齐，规则未实现 |

---

## 2. `内圈循环-收敛定稿 §七` 待修缺口复核（都已修）

| §七 列的缺口 | 当时状态 | 现读代码 | 结论 |
|---|---|---|---|
| P4 空文本误返 completed | loop.py:70 返回 completed | `if not turn.tool_calls: if content.strip()→completed; else 回滚+empty_retries`(277-289) | ✅ 已修 |
| 缺 awaiting_confirmation 状态 | 缺 | `LoopResult.status` 含 awaiting_confirmation | ✅ 已修 |
| call_model 缺重试 | 缺 | `_call_model_with_retry` 指数退避 | ✅ 已修 |
| repair 仅 assert | 仅 assert | `repair_messages` 每轮跑 | ✅ 已修 |
| 缺 stall 守卫 | 缺 | 相同签名 ×3 → failed/stall(310-317) | ✅ 已修 |
| 预算墙钟 deadline_s（nit） | 在 budget.py | 已无 deadline_s/墙钟 | ✅ 已修 |
| P3 子 agent 错误分型 | 污染父 max_tool_failures | `subagent.py` 用 reason 区分 model_error/empty/stall/tool_failures，interrupted 单独处理 | 🟢 基本到位 |
| 预算 rehydrate | 缺 | `budget.restore(snapshot)` + Boundary 持久 | ✅ 已修 |

**结论**：现役代码 = 定稿 §六·补 + §七「下一步」清单的实现版。理念审计这一项可以放心。

---

## 3. 真接缝 vs 桩（组合根 `build_tool_subsystem` 实况）

| 接缝 | 引擎默认 | 真实件 | 真实件状态 |
|---|---|---|---|
| 上下文组装 | LayeredContextAssembler(桩) | ParkContextAssembler | 🟢 已实现，注入即用 |
| 闸 | DefaultGate(桩) | CatalogGate | 🟢 已实现（权限码已配 device/record/life/knowledge:read + device:control） |
| 控制 | —（需注入） | ProposalControlCapability | 🟢 逻辑实现，**接 backend** |
| 后端 | —— | FakeBackendClient / **ProdApiBackendClient** | 🔴 默认 Fake；真后端要接（getDevicePage/sycPointParamType/deviceCtrl） |
| verify | NullVerifier(桩) | —— | 🔴 真 validation_policy 未写 |
| 业务域叶子 | —— | facility/records 有后端路由；life(会议/停车/餐厅) 桩 handler | 🟡 多数桩 |

---

## 4. 真正待办（按定稿分期，非理念问题）

1. **【硬阻塞】deviceCtrl 幂等化** — 定稿两处标红：后端加 `commandId`+幂等前，**不可逆控制不能上线**（崩溃重发会双发）。`devicectrl-接口改造建议.md` 是发后端的改造单，未落。
2. **后端真化** — `ProdApiBackendClient` 接真接口；各域叶子从桩换真（食堂/停车/会议查询）。
3. **verify 真化** — 写各能力 `validation_policy`（执行后校验 + 控制读回对账），替 NullVerifier。
4. **商业/导航两域** — 代码里无工具（见 `00 §1.5`），需新增 manifest + 适配。
5. **测试** — 见 `01`；§0.5 引擎层测试现在就能写（代码已对齐，可放心锁行为）。

---

## 5. 对收敛包的回写

`00 §1.5` 的三个「理念对齐疑点」审计结果：
- ✅ **plan DAG 残留** → 无残留，代码已是 todo 列表，疑点解除。
- ✅ **loop 事务化是否落全** → 已落全，且超出定稿 §七 清单。
- ✅ **assembler 功能标签非缓存** → 已对齐（含丢弃/裁剪/context awareness）。

**所以 `00 §1.5` 的「覆盖度上调带前提」前提已满足**：理念审计通过，旧代码可放心作为基线复用；真正要补的是「后端真化 + verify + 测试 + 商业/导航域」，全是分期工程不是返工。
