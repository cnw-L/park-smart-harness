# 证据捕获正则误报

## 症状

`evidence_capture.py` 把"命令字符串里 merely 提到验证命令"的调用也捕获了（例：`claude -p "...pytest -q..."`，pytest 只出现在提示词文本里，2026-09-06 A/B 试验 14:30 记录）。

## 根因

匹配是对整条命令做正则搜索，不解析 shell 结构；非执行位的字符串引用无法区分。

## 处置

可接受的低频噪音（宁可多记不漏记——记录的是事实而非判定）；若变严重，按 shell 分段（`&&`/`;`/`|` 切分后段首匹配）收紧。暂不动作。
