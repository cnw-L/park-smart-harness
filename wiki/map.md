# wiki/map.md — 领域地图（稳定页：更新制）

> **只放指针，不放内容**——本页是导航缓存，不是 Current：项目当前状态的权威更新通道是 SP-19 Calibration（红线 1）。
> 更新制：模块结构性变化（新增/拆分/退役）随 SP-19 收口一并改写本页，保持当前真实；不能改写的整行标 `[已过时：YYYY-MM-DD，原因]`，不删行、不追加重复行。

## 模块索引

| 模块/域 | 入口指针 | Current 文档指针 | 关联 pattern |
|---|---|---|---|
| （示例）认证域 | `src/auth/` | `docs/current/auth.md` | `patterns/token-refresh.md` |

（接入后由执行 agent 随真实收口逐行补充；空表不是缺陷。多 agent 开工前按本表判域，再查 `changes/` 的 OPEN 记录防撞域。）
