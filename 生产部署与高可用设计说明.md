# park-smart-harness · 生产部署与高可用设计说明

> 版本:v1.0 · 2026-07-10 · 状态:**初稿(讨论归纳)**
>
> 本文把 2026-07-10 关于「harness 要上生产,Redis/PG/Milvus 等怎么部署、部署多少个、怎么做高可用」的讨论,结合代码现状与权威资料,归纳成一份可执行的部署设计说明。**讨论优先、配置次之、代码缺口末之**——先讲清各件做什么、怎么冗余、崩了谁来顶,再落到本仓库现在的代码差在哪、要改什么。
>
> 配套阅读:[项目设计文档](项目设计文档.md)(建什么)、[项目落地文档](项目落地文档.md)(怎么建/何时建)、[项目资料总览](项目资料总览.md)(资料盘点)。
>
> **核实说明**:本文涉及的外部技术断言(asyncpg `target_session_attrs`、Redis Sentinel 机制、Redis Cluster 哈希槽、Patroni)均经官方文档核实,来源见 §10。代码引用均按本仓库当前代码核对到行。Milvus standalone/cluster 为既定部署模式,按通行认知陈述。

---

## 0. 阅读指南

| 你想了解 | 看哪节 |
|---|---|
| 演示环境和生产环境的根本差别 | §1 出发点 |
| Redis/PG/Milvus 各存什么、崩了会丢什么 | §2 存储角色与现状 |
| 主从/主备/哨兵/集群/分布式/分片到底是什么 | §3 基础概念 |
| Redis 怎么部署、为什么推荐哨兵不推荐集群 | §4 Redis 部署 |
| Postgres 怎么部署、自动故障转移靠谁 | §5 Postgres 部署 |
| Milvus 单机还是分布式 | §6 Milvus 部署 |
| 应用进程怎么摆、负载均衡怎么配 | §7 应用层 |
| 现在的代码离生产差在哪(承重) | §8 关键约束与代码缺口 |
| 到底要多少台机器 | §9 节点规模 |
| 哪些断言查过出处 | §10 已核实事实与来源 |
| 按什么顺序落地 | §11 落地路径建议 |

---

## 1. 出发点:演示环境与生产环境的根本差别

当前 `scripts/demo_server.py` 演示服务用的是**内存存储**(`InMemoryConversationStore`,demo_server.py:67),Redis 和 Postgres **根本没接进去**。演示能跑、能控制真机(通过 prod-api 与 Basic Auth + Tailscale),但:

- 进程一重启,所有会话状态全丢;
- 单进程,崩了就没了,没有冗余;
- 内存里放,放不下也查不了历史。

生产环境要解决三件事,这正是本文的主题:

1. **状态外置**:把会话状态、提案、幂等记录、审计从进程内存搬到独立的 Redis / Postgres / Milvus,让应用进程变成无状态(任意一台都能服务任意一个会话)。
2. **冗余与故障转移**:每类存储都要有多副本,主节点崩了要有机制自动选一个从节点顶上,应用尽可能无感。
3. **横向扩展**:应用进程能多开几台分摊流量,存储也能按需扩容。

下面先讲清楚三类存储各自的角色,这是所有部署决策的根。

---

## 2. 存储角色与现状

harness 的存储分三层,各司其职,崩了的后果也不同。以下角色按本仓库代码核对。

### 2.1 Redis —— 热会话状态 + 待确认提案(强一致、易失)

- **会话热状态**:`RedisConversationStore`(`src/agent_loop/redis_store.py`)。每个会话(thread_id)在 Redis 里用**两个列表**存:
  - `{prefix}:{thread_id}:messages` —— 消息序列(redis_store.py:90)
  - `{prefix}:{thread_id}:boundaries` —— 轮次边界(redis_store.py:93)
  - 写入时用 **MULTI/EXEC 事务**把两个列表的 RPUSH 原子打包,保证不出现「消息写了但边界没写」的撕裂(redis_store.py:147-151)。
  - 挂起确认时给两个 key 设 TTL;正常活跃会话则 PERSIST 移除 TTL(redis_store.py:8-9)。
- **待确认提案**:`RedisProposalStore`(`src/agent_tools/proposal.py:94`)。同步 redis 客户端,存待用户确认的控制提案。
- **崩了会怎样**:Redis 全崩 = 当前活跃会话的最近上下文丢失。但有 Postgres 审计兜底(见 2.2),可从审计回放重建关键边界。**控制路径的 pending_batch 在提交时强制写 PG 审计(fail-closed),所以即便 Redis 丢了,已确认的控制动作不会丢账。**

> 一句话:Redis 是「快但易失」,管当前正在进行的对话;丢了能从 PG 审计恢复控制边界,但对话的中间措辞会丢。

### 2.2 Postgres —— 幂等账本 + 只增审计(持久、控制路径承重)

- `PgStore`(`src/agent_loop/pg_store.py`):
  - `agentloop_idem` 表:幂等键去重(主键 idem_key,`ON CONFLICT DO NOTHING`),防止同一动作重复执行。
  - `agentloop_audit` 表:只增(JSONB)审计日志,记录每一步发生了什么。
- `AuditedConversationStore`(`src/agent_loop/audited_store.py`)把 Redis 热存 + PG 审计组合起来:
  - **控制边界(pending_batch)提交 → PG 审计 fail-closed**:写不进 PG 就当失败,绝不含糊。
  - **对话内容提交 → best-effort**:尽力写,写不进不阻断对话。
  - `resolve_pending`(解决待确认)→ 一律 fail-closed。
- **崩了会怎样**:PG 不可用 = **控制路径直接卡住**(fail-closed),这是有意的——宁可不能控制设备,也不能漏记一笔控制账。对话路径会降级为 best-effort 继续走。

> 一句话:PG 是「慢但可靠」,管控制动作的账本和幂等;它的可用性直接决定能不能安全地控制设备。

### 2.3 Milvus —— RAG 向量库(检索、可重建)

- `src/harness_rag/milvus/client.py`:存知识库的向量索引,供检索增强生成(RAG)。
- **崩了会怎样**:Milvus 不可用 = 检索召回失败,模型这一轮拿不到相关知识,但**对话和控制不中断**(RAG 是增强项,不是承重路径)。向量可以从原始文档重新 embedding 重建,所以 Milvus 的数据持久性压力最小。

> 一句话:Milvus 是「锦上添花」,崩了系统还能跑只是答得没那么准;数据能从源文档重建。

### 2.4 三者崩了的影响一览

| 存储 | 崩了的影响 | 可否恢复 |
|---|---|---|
| Redis | 丢当前活跃会话的最近上下文 | 控制边界可从 PG 审计回放;对话措辞丢 |
| Postgres | **控制路径卡住**(fail-closed) | 不丢账;恢复后即续 |
| Milvus | 检索召回失败,答得变差 | 可从源文档重建索引 |

这张表是后面所有冗余设计的依据:**PG 的可用性最关键(承重),Redis 次之(强一致但可降级),Milvus 最轻(可重建)**。

---

## 3. 基础概念:主从、主备、哨兵、集群、分布式、分片

讨论部署绕不开这几个词,先把它们讲清楚。用「一个团队」打比方。

### 3.1 主从 / 主备(主写,从备,保持同步)

**这是所有高可用的地基。** 一份数据放多台机器:

- **主(master / primary)**:唯一负责写。所有写操作都走它。
- **从(slave / replica / standby)**:从主那里同步数据,跟着主变。平时可以接读请求分担压力,也可以纯待命。
- 主崩了,把一个从提升成新主,系统继续可用——这就是「故障转移(failover)」。

> 比方:一个团队只有一个组长能拍板(主),其他人(从)都有一份一样的会议记录跟着更新。组长不在了,选个记录最全的人顶上。

- Redis:主从复制(replication)是内置基础功能。
- Postgres:叫「流复制(streaming replication)」,也是内置的,主把 WAL 日志流式发给从。

**注意:主从复制本身只解决「数据有多份」,不解决「主崩了自动换」**。自动换主需要额外的机制——这就是哨兵或 Patroni 干的事(见 3.3、§5)。

### 3.2 副本(replica)

就是「从」的另一个叫法。「3 个副本」通常指「1 主 + 2 从」共 3 份数据,或视语境指 3 份完整的数据拷贝。本文统一用「1 主 + N 从」的说法避免歧义。

### 3.3 哨兵(Sentinel)—— 给主从加「自动故障转移」

哨兵是 **Redis 专属**的机制(别的软件有等价物,如 Postgres 的 Patroni)。它解决「主崩了谁来自动换」:

- 哨兵是**独立的进程**,不存业务数据,只盯着主从节点。默认端口 **26379**(和存数据的 6379 不同)。
- 多个哨兵(通常 3 个)**互相通信 + 各自探测**主节点:
  1. 一个哨兵发现主连不上 → 标记为「**主观下线(sdown, Subjectively Down)**」(只是我一人觉得它挂了)。
  2. 达到**法定人数(quorum)**——足够多哨兵都同意它挂了 → 升级为「**客观下线(odown, Objectively Down)**」(大家公认挂了)。
  3. 哨兵们**投票选一个领头的**(需**多数(majority)**哨兵同意),由它执行故障转移。
  4. 领头哨兵挑一个从,提升为新主,让其他从改连新主,并通知应用。
- **为什么至少 3 个哨兵**:故障转移要多数同意。2 个哨兵时若其中一个自己也挂了,剩 1 个凑不够多数,就换不了主。3 个挂 1 个还剩 2 个能凑多数。所以哨兵数取**奇数**(3 或 5),容忍 `(n-1)/2` 个挂。

> 比方:团队配 3 个「监督员」(哨兵),他们不干活只盯组长。组长失联了,要过半监督员(2 个)都确认「组长真挂了」,才开会选个新组长。为什么 3 个不是 2 个?因为监督员自己也可能请假,2 个里挂 1 个就只剩 1 个,凑不过半了。

**关键**:哨兵是**独立的进程和配置**,不是「把存数据的 redis-server 改个启动参数就变成哨兵」。它有自己的 `sentinel.conf`,用 `redis-sentinel sentinel.conf` 或 `redis-server --sentinel` 启动。**哨兵本身不用我们写代码**——它是 Redis 自带的;应用侧需要做的是:用「哨兵感知」的客户端连法(连哨兵问主在哪),而不是写死一个 Redis 地址。

### 3.4 集群(Cluster)—— 多节点当一个用 + 分片

「集群」这个词在不同语境下意思不同,最容易混淆。这里分两层:

- **广义集群**:同一套软件跑在多台机器上,对外像一个整体。主从 + 哨兵其实也算一种「集群」(多节点冗余)。
- **狭义集群(Redis Cluster 专指)**:Redis 的一种**分片**部署模式。数据不是每台都存全量,而是**切分成 16384 个哈希槽(hash slot)**,每台节点负责其中一部分槽。存一个 key 时,用 `CRC16(key) % 16384` 算出它落在哪个槽、归哪个节点。好处是容量和吞吐能随节点数横向扩展。

> 比方:主从像「3 个人各有一份完整会议记录」(冗余,为的是不丢);集群分片像「100 份档案分给 3 个人各管 33 份」(分工,为的是装得多、查得快)。两者目的不同,可以叠加(分片的每一片再做主从)。

### 3.5 分布式(Distributed)—— 不同部件散在不同机器上协作

「分布式」是比「集群」更大的概念:把系统的不同部件放在不同机器上协同工作。集群(同质节点当一个整体)是分布式的一种特例;分布式还包括「不同质部件跨机协作」。

> 比方:集群 = 3 个一模一样的收银台;分布式 = 收银台、仓库、财务室分别在不同楼层,互相配合完成一笔交易。

Milvus 的「分布式部署」就是这个意思:它把根协调器、查询协调器、数据协调器、索引协调器 + 各自的 worker 节点 + etcd + 对象存储 + 消息队列,散成多个独立服务跨机协作(见 §6)。

### 3.6 分片(Sharding)—— 数据切片

分片是「把数据切分成片,分到不同节点」这个**动作/策略**,和集群、分布式是**正交**的概念:

- 可以做分片但不集群(手动分片到几个独立实例);
- 也可以集群但不分片(主从冗余,每台存全量)。
- Redis Cluster = 集群 + 分片(把哈希槽分到节点)。

> 比方:分片是「把蛋糕切成几块」这个动作;至于切完怎么摆(都放一个盘子=冗余,分到几个盘子=集群分片),是另一回事。

### 3.7 一张表收束

| 概念 | 解决什么 | Redis 语境 | Postgres 语境 |
|---|---|---|---|
| 主从/主备 | 数据有多份(冗余) | 内置 replication | 内置流复制 |
| 副本 | 「从」的别称 | replica | standby |
| 哨兵 | 主崩自动换(故障转移) | Redis Sentinel(3.3) | 无内置,用 Patroni(§5) |
| 集群 | 多节点当一个整体 | 主从+哨兵也算;狭义指 Cluster | 流复制组 |
| 分片 | 数据切片扩容 | Redis Cluster 哈希槽 | 无内置(需 citus 等) |
| 分布式 | 部件跨机协作 | — | Patroni+etcd 即分布式 HA |

---

## 4. Redis 部署

### 4.1 推荐:主从 + 哨兵模式

**1 主 + 2 从 + 3 哨兵,共 6 个进程/节点:**

| 角色 | 数量 | 端口 | 作用 |
|---|---|---|---|
| 主 master | 1 | 6379 | 读写(写只走主) |
| 从 replica | 2 | 6379 | 同步主、分担读;主崩可被提升 |
| 哨兵 sentinel | 3 | 26379 | 探测+投票+自动故障转移 |

- **为什么 2 个从**:主崩提升 1 个从成新主后,还要剩至少 1 个从保持冗余,否则又成单点。
- **为什么 3 个哨兵**:见 3.3,凑多数投票,容忍 1 个挂。
- 哨兵可以和主从**同机部署**(3 台机器各跑 1 主/从 + 1 哨兵),省机器;生产强一致场景建议哨兵独立或至少分散。

### 4.2 为什么不推荐 Redis Cluster(分片模式)

这不是「集群不好」,而是**本仓库现在的代码和 Cluster 的分片约束冲突**:

- Redis Cluster 要求:**多 key 操作(事务 MULTI/EXEC、Lua 脚本)涉及的所有 key 必须落在同一个哈希槽**,否则报 `CROSSSLOT` 错误。
- 让多个 key 落同槽的办法是**哈希标签(hash tag)**:key 里用 `{...}` 括起一段,只对括号内算哈希。例如 `user:{123}:profile` 和 `user:{123}:account` 都只对 `{123}` 算哈希,落同槽。
- 本仓库 `redis_store.py` 每个会话用**两个 key**:`{prefix}:{thread_id}:messages` 和 `{prefix}:{thread_id}:boundaries`(redis_store.py:90、93),并用 **MULTI/EXEC 跨这两个 key 做原子写**(redis_store.py:147-151)。
- 这两个 key **没有加哈希标签**,在 Cluster 下大概率落不同槽 → MULTI/EXEC 直接失败。

> 所以:要么上 Cluster 就得先给两个 key 加 `{thread_id}` 哈希标签(改 `redis_store.py:90/93` 的 key 格式,且 proposal 的 key 也得一并处理),要么就别上 Cluster,用主从+哨兵。**当前规模下,主从+哨兵的容量和吞吐足够,没必要为分片付出改 key 格式的代价。** 真到了单 Redis 扛不住的量级,再考虑 Cluster + 哈希标签改造。

### 4.3 应用侧的代码缺口(承重)

现在的 Redis 客户端是**写死单地址**的,不感知哨兵:

- `redis_store.py:85-86`:`aioredis.Redis.from_url(self._url, ...)`,`_url` 默认 `redis://localhost:6379/0`。
- `proposal.py:94`:`redis.Redis.from_url(self._url, ...)`,同样写死单地址(且和会话共用 db 0)。

`from_url` 连的是**一个固定地址**,主崩了它不知道新主在哪,不会自动切换。上哨兵后,应用侧要改成**哨兵感知客户端**:

```python
# 大致写法(redis-py 内置 Sentinel 支持,不用自己写故障转移逻辑)
from redis.sentinel import Sentinel
sentinel = Sentinel([("sentinel1", 26379), ("sentinel2", 26379), ("sentinel3", 26379)])
master = sentinel.master_for("mymaster", socket_timeout=0.5)   # 自动拿到当前主
# 异步版:redis.asyncio.Sentinel
```

三种落地选择(任选其一):

1. **改代码**(最直接):把 `redis_store.py` / `proposal.py` 的 `from_url` 换成 `Sentinel(...).master_for(...)`,读走 `slave_for`。改动小,但要动两个文件。
2. **加代理**:在 Redis 前面放一层代理(如 `redis-shard`/`twemproxy` 不支持哨兵;支持哨兵的如 `predixy`、或云厂商的代理),应用仍用 `from_url` 连代理,代理负责找主。应用零改动,但多一层运维。
3. **用云服务**:直接用阿里云/腾讯云/AWS 的托管 Redis(自带哨兵语义的 HA),给一个连接串,故障转移云厂商包办。应用零改动,最省心,但要花钱且数据上云。

### 4.4 Redis 的强一致注意点

- **会话热状态是强一致需求**:应用的读写都应走主(master),不要写完主立刻读从(从有同步延迟,可能读到旧值)。
- 这意味着哨兵模式下,会话读写**都连 master_for**,从只用于非关键读或备份。
- proposal 同理,待确认提案的读写也走主。

---

## 5. Postgres 部署

### 5.1 流复制:内置,主从同步

- Postgres 的主从复制叫**流复制(streaming replication)**,内置功能:主把 WAL(预写日志)流式发给从,从重放。
- **同步级别可选**:
  - **异步(async)**:主写完就返回,不等从确认。性能好,但主崩的瞬间,未同步到从的少量事务会丢。
  - **同步(sync)**:主等至少一个从确认收到才返回(`synchronous_standby_names` 配置)。强一致,性能略降,主崩不丢已确认事务。
  - 生产建议:**至少 1 个同步从**(保证控制账不丢)+ 1 个异步从(分担/冗余)。

### 5.2 自动故障转移:Postgres 没内置,要靠 Patroni 或云

**这是和 Redis 最大的不同**:Redis 有哨兵(自带),**Postgres 没有内置的自动故障转移**。主崩了,流复制不会自己选新主,要么人工介入,要么上外部工具:

- **Patroni + etcd**(主流开源方案):
  - Patroni 是个守护进程,和 PG 同机部署,管 PG 的启停和主从角色。
  - 它用**分布式配置存储(DCS)**——etcd / Consul / ZooKeeper 任选——做**领导者选举**:同一时刻只有持有 leader 锁的那个 PG 节点才是主,其他是从。
  - 主崩 → leader 锁过期 → 一个从抢到锁 → Patroni 把它提升为新主 → 其他从改连新主。这就是 PG 版的「哨兵」。
  - etcd 自己也是个小集群(3 节点,Raft 共识),所以要单独部署 3 个 etcd。
- **repmgr**:较轻,做复制管理和故障转移,但不如 Patroni 健壮。
- **云托管**:用云厂商的托管 PG(RDS 等),故障转移云厂商包办,省掉 Patroni+etcd 的运维。

### 5.3 应用侧:多主机连接串 + target_session_attrs(已核实)

应用怎么知道主在哪、主换了怎么自动跟?有两个层次:

- **连接池层(PgBouncer)**:在 PG 前面放 PgBouncer(2 个做 HA),应用连 PgBouncer,PgBouncer 连主。主切换时 PgBouncer 重连。应用零改动。
- **驱动层(asyncpg 多主机 DSN)**:asyncpg 从 **0.28.0** 起支持 `target_session_attrs` 参数,语义和 libpq 一致。把 DSN 写成多主机:
  ```
  postgresql://user:pass@host1:5432,host2:5432,host3:5432/smart_park?target_session_attrs=read-write
  ```
  - `read-write` 含义:**只连「能接受读写事务」的那个节点**(即不在 hot standby、`default_transaction_read_only=off` 的主)。驱动依次试 host,自动连到当前主。
  - 主切换后(Patroni 提升新主),新主变成 read-write,旧主变 standby,驱动下次连接自动连到新主。**纯配置改动,不用改代码。**
  - 六种取值:`any`(任意能连的,默认)、`read-write`(必须可读写=主)、`read-only`、`primary`(非 standby)、`standby`、`prefer-standby`。

**当前代码缺口**:`pg_store.py:89` 用 `asyncpg.create_pool(self._dsn)`,DSN 是单主机(`SPA_STORAGE__POSTGRES_DSN`,默认 `postgresql://postgres:postgres@localhost:5432/smart_park`)。要上 HA,把 DSN 换成上面的多主机串 + `target_session_attrs=read-write` 即可,**前提是 asyncpg ≥ 0.28.0**(老版本不支持,需先升级或改用 PgBouncer 方案)。

> 核实来源:asyncpg 官方 API 文档(0.28.0 加入 `target_session_attrs`)+ PostgreSQL libpq 文档(参数语义)。详见 §10。

### 5.4 PG 的可用性是控制路径的命脉

回顾 §2.2:控制路径提交 pending_batch 是 **fail-closed**——PG 写不进就当失败。所以 **PG 的可用性直接决定「能不能安全控制设备」**。这是 PG 必须做 HA、且至少 1 个同步从的根本原因:不能让控制账因为单点 PG 崩了而卡死或丢失。

---

## 6. Milvus 部署

Milvus 有两种部署模式,按规模选:

### 6.1 Standalone(单机模式)

- 所有组件(协调器、worker、etcd、对象存储)打包在**一个进程/容器**里。
- 适合开发、测试、小规模(单机数据量不大)。
- **无 HA**:单点,崩了检索就断;但数据能从源文档重新 embedding 重建,影响可控(§2.3)。

### 6.2 Cluster(分布式模式)

- 拆成多个独立微服务:**根协调器(root coord)、查询协调器(query coord)、数据协调器(data coord)、索引协调器(index coord)** + 各自的 worker 节点,外加依赖:**etcd(元数据)、MinIO/S3(对象存储)、Pulsar/Kafka(消息日志)**。
- 横向可扩展,容量和吞吐随节点增加;适合生产大规模。
- 代价:依赖多(etcd / MinIO / Pulsar 各自要运维和 HA),部署复杂度高。

### 6.3 给 harness 的建议

- **起步**:RAG 不是承重路径(§2.3),先用 **Standalone** 跑,单点风险可接受(崩了重建,不影响控制)。
- **上量后**:数据量大或检索 QPS 高时再上 **Cluster**;此时 etcd 可以和 PG 那套 etcd 合并(如果都用 etcd 做 DCS),省一套运维。
- Milvus 自身的高可用细节(各协调器多副本、worker 弹性)按官方 distributed helm chart 部署即可,本文不展开。

---

## 7. 应用层

应用进程(`scripts/demo_server.py` 那个 FastAPI 服务)要变成无状态、可横向扩展:

### 7.1 拓扑:2 负载均衡 + 3 无状态应用

```
                    ┌──────── 负载均衡器 ×2(HA 主备 / 双活)────────┐
                    │  keepalived/VIP 或云 LB                        │
                    └───────┬───────────────┬───────────────┬───────┘
                            │               │               │
                     ┌──────▼──┐      ┌─────▼───┐     ┌─────▼───┐
                     │ 应用 ×3 │      │ 应用 ×3 │     │ 应用 ×3 │   ← 无状态
                     │ (FastAPI)│      │(FastAPI)│     │(FastAPI)│     状态在 Redis/PG
                     └────┬────┘      └────┬────┘     └────┬────┘
                          └──────────────┬─┴───────────────┘
                          ┌──────────────▼──────────────┐
                          │  Redis(哨兵) / PG(Patroni) / Milvus  │
                          └─────────────────────────────┘
```

- **3 个应用进程**:无状态(状态都外置到 Redis/PG),任意一个都能服务任意会话。3 个是为了滚动发布时还能有 2 个接客,以及分担 CPU/内存。
- **2 个负载均衡器**:互为主备(keepalived 漂 VIP)或双活(云 LB),避免 LB 自己成单点。LB 把请求按会话亲和(session sticky,按 thread_id 哈希)分发——见 §8.2 的单 writer 约束。

### 7.2 为什么应用要无状态

状态外置后,任意应用实例崩了,LB 把流量转到其他实例,会话从 Redis 恢复继续——这就是「无状态」的意义:实例是可丢弃的、可随意扩缩的。

---

## 8. 关键约束与代码缺口(承重)

这是本文最该被工程化对待的一节。每条都直接影响部署能不能真正 HA。

### 8.1 Redis Cluster 与 MULTI/EXEC 冲突(已核实)

- **事实**:`redis_store.py` 用 MULTI/EXEC 跨两个无哈希标签的 key(redis_store.py:90/93/147-151)。Redis Cluster 要求多 key 事务的同槽约束,会报 CROSSSLOT。
- **决策**:当前**不上 Redis Cluster**,用主从+哨兵。将来上 Cluster 必须先给两 key 加 `{thread_id}` 哈希标签(并同步处理 `proposal.py` 的 key)。
- 来源:Redis Cluster 官方文档(§10)。

### 8.2 单 writer / 单线程假设(代码注释已写明)

- **事实**:`redis_store.py:11-13` 注释明确:`resolve_pending` 用 DELETE+RPUSH 序列,依赖**「每个 thread_id 同一时刻至多一个 pending 集、由上层保证单 writer」**。这不是并发安全的原子操作。
- **部署影响**:多应用实例下,若同一 thread_id 的请求被分发到不同实例并发处理,DELETE+RPUSH 会冲突,可能丢消息或边界错乱。
- **对策**:**会话亲和(session sticky)**——LB 按 thread_id 哈希把同一会话固定到同一应用实例;或用**分布式锁**(每 thread_id 一把 Redis 锁)串行化。会话亲和最简单,先上。

### 8.3 demo 未接 Redis/PG(接线缺口)

- **事实**:`demo_server.py:67` 用 `InMemoryConversationStore`,`_model = OpenAIModelCaller(timeout=40.0)`(demo_server.py:70),**完全没接 Redis/PG**。
- **对策**:演示用内存无妨;生产入口要换成 `AuditedConversationStore(RedisConversationStore(...), PgStore(...))`,并配好 `ASSISTANT_REDIS_URL` / `SPA_STORAGE__POSTGRES_DSN` 等环境变量。

### 8.4 Redis 客户端不感知哨兵(已列)

- `redis_store.py:85-86`、`proposal.py:94` 写死 `from_url` 单地址。上哨兵要改哨兵感知客户端或加代理或上云(§4.3)。

### 8.5 PG 客户端不感知主切换(已列)

- `pg_store.py:89` 单主机 DSN。上 Patroni 要换多主机 DSN + `target_session_attrs=read-write`(需 asyncpg ≥ 0.28.0),或前置 PgBouncer(§5.3)。

### 8.6 PG 可用性是控制路径命脉(已述)

- fail-closed 语义(§2.2、§5.4):PG 不可用 = 控制路径卡住。PG 必须 HA + 至少 1 同步从,否则单点 PG 崩 = 无法安全控制设备。

---

## 9. 节点规模

下面给两档规划基线,**不是硬性规定**,按实际流量和数据量调整。所有计数均为「进程/节点」级,可同机合并省机器(但合并会牺牲隔离性)。

### 9.1 最小 HA(能容忍单点故障,约 13–15 节点)

| 组件 | 节点数 | 说明 |
|---|---|---|
| Redis | 6 | 1 主 + 2 从 + 3 哨兵(哨兵可与主从同机) |
| Postgres | 3 | 1 主 + 1 同步从 + 1 异步从,Patroni 同机,故障转移靠 asyncpg 多主机 DSN + 手动/简易 |
| etcd | 3 | Patroni 的 DCS(可与 PG 同机,但建议独立或共享) |
| Milvus | 1 | Standalone(非承重,接受单点) |
| 应用 | 3 | 无状态 |
| 负载均衡 | 2 | 主备 |
| **合计** | **~18** | 合并同机可压到 ~13–15 |

> 这档里 Milvus 单点、PG 故障转移可能偏简陋(纯靠 target_session_attrs + 手动提升或 repmgr),适合先跑起来验证。

### 9.2 标准生产(全链路 HA,约 25–40 节点)

| 组件 | 节点数 | 说明 |
|---|---|---|
| Redis | 6 | 1 主 + 2 从 + 3 哨兵,哨兵独立 |
| Postgres | 3 | 1 主 + 2 从,Patroni 同机 |
| etcd | 3 | 独立 Raft 集群(Patroni DCS,可与 Milvus 共享) |
| PgBouncer | 2 | 连接池,HA 主备(可选) |
| Milvus | 7–10 | Cluster 分布式:协调器 ×3 + worker 若干 + 依赖(MinIO、Pulsar 各自 HA) |
| 应用 | 3–5 | 无状态,按 CPU 扩 |
| 负载均衡 | 2 | 主备/双活 |
| **合计** | **~25–40** | Milvus 分布式占大头 |

### 9.3 选型一句话

- **小规模先跑通** → 最小 HA,Milvus 单机,PG 故障转移靠 target_session_attrs。
- **真上量、要稳** → 标准生产,Milvus 分布式,PG 上 Patroni+etcd,Redis 哨兵独立。

---

## 10. 已核实事实与来源

本文以下断言经官方文档核实(2026-07-10,通过官方站点抓取):

| 断言 | 核实来源 | 关键结论 |
|---|---|---|
| asyncpg 支持 `target_session_attrs` | asyncpg 官方 API 文档 | 0.28.0 起加入;六种取值与 libpq 一致;支持多主机 DSN |
| `target_session_attrs` 参数语义 | PostgreSQL libpq 文档(libpq-connect) | `read-write` = 主(非 hot standby 且 read_only=off);支持 `host1:port,host2:port` 多主机 |
| Redis Sentinel 机制 | redis.io Sentinel 文档 | 独立进程、端口 26379、quorum、sdown→odown、majority 投票选领头、自动故障转移 |
| Redis Cluster 哈希槽 | redis.io Cluster 文档 | 16384 槽、CRC16 取模、多 key 事务须同槽、`{}` 哈希标签 |
| Patroni 是 PG HA 方案 | Patroni readthedocs | "A Template for PostgreSQL HA with ZooKeeper, etcd or Consul",用 DCS 做领导者选举+故障转移 |

**未单独核实、按既定认知陈述**:Milvus standalone/cluster 两种部署模式及各自组件构成(官方文档抓取遇重定向,未能直接引用;此为 Milvus 长期稳定的既定模式,建议落地时以官方 helm chart 为准)。

**代码引用**:均按本仓库当前代码核对到行(redis_store.py:8-13/85-86/90/93/147-151;proposal.py:94;pg_store.py:89;demo_server.py:67/70)。

---

## 11. 落地路径建议

按依赖与风险排序,从易到难、从承重到非承重:

1. **先接线(让 demo 用上 Redis/PG)**:把 `demo_server.py` 的 `InMemoryConversationStore` 换成 `AuditedConversationStore(RedisConversationStore, PgStore)`,配环境变量。本地用 docker-compose 起 Redis/PG/Milvus 单机版即可。**这步不动 HA,只验证状态外置能跑。**
2. **PG 上 HA(承重优先)**:1 主 + 1 同步从 + 1 异步从;DSN 换多主机 + `target_session_attrs=read-write`(确认 asyncpg ≥ 0.28.0);暂可不上 Patroni,靠 target_session_attrs + 手动提升过渡。PG 是控制路径命脉,先保它。
3. **Redis 上哨兵**:1 主 + 2 从 + 3 哨兵;改 `redis_store.py`/`proposal.py` 为哨兵感知客户端(或加代理/上云)。会话读写走主。
4. **应用多实例 + 会话亲和**:3 应用 + 2 LB;LB 按 thread_id 哈希做会话亲和,满足单 writer 假设(§8.2)。
5. **PG 上 Patroni+etcd(自动故障转移)**:etcd 3 节点 + Patroni 同机;验证主切换时应用通过 target_session_attrs 自动跟随。
6. **Milvus 视量级而定**:小量级保持 Standalone;上量再转 Cluster(可与 PG 共享 etcd)。
7. **(可选)Redis Cluster 改造**:仅当单 Redis 扛不住时,先给 `redis_store.py:90/93` 两 key 加 `{thread_id}` 哈希标签(并处理 proposal key),再上 Cluster。

> 节奏建议:1–2 是「能上生产」的最低门槛;3–5 是「稳」的门槛;6–7 是「扛量」的门槛。别一上来就全上,先承重( PG)后非承重(Milvus),先冗余(主从)后自动(哨兵/Patroni)。

---

*本文为讨论归纳初稿,随落地推进迭代。代码缺口(§8)以本仓库当前代码为准;若代码已改,请同步更新本文引用行号。*
