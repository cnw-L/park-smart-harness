# park-smart-harness · 项目落地文档

> 版本:v1.0 · 2026-07-08 · 状态:**定稿(权威综合)**
>
> 本文是 park-smart-harness 的**落地实施权威**,把 `harness-park-design/实施计划.md` 的 Gate 决策、各分册的实施计划、代码现状、评测与降级策略凝成一份完整的"怎么建、何时建、怎么验、怎么收"的路线。配套阅读:[项目设计文档](项目设计文档.md)(建什么)、[项目资料总览](项目资料总览.md)(资料盘点)。
>
> 本文不重复设计的"为什么"——那在《项目设计文档》;本文专注"何时做、按什么顺序、怎么验证、靠什么收尾"。

---

## 0. 阅读指南

| 你想了解 | 看哪节 |
|---|---|
| 怎么决策取舍 | §1 落地原则 |
| 现在到哪了 | §2 现状盘点 |
| 整体怎么推进 | §3 落地总策略 |
| 分几步、每步干啥 | §4 实施阶段 Phase 0-6 |
| 哪些关键决策定了、为什么 | §5 Gate 决策记录 G1-G6 |
| 怎么验证不退步 | §6 评测方案 |
| 各子系统挂了怎么办 | §7 降级矩阵 |
| 卡在哪、等什么 | §8 硬依赖与风险登记 |
| 怎么测 | §9 测试策略 |
| 怎么部署 | §10 部署与配置 |
| 跟腾讯 V6 怎么对齐 | §11 V6 对齐 |
| 每阶段怎么算完 | §12 里程碑与验收 |

---

## 1. 落地原则

七条贯穿所有取舍:

1. **治理优先(governance-first)**:每个决策点先问"该不该做"再问"怎么做"。deny-by-default,fail-safe,失败不当成功。任何无治理的捷径都是债。
2. **评测优先(eval-first)**:先有测试集再写功能。改了行为先跑离线编排测试集,绿了才合。不在没有回归基线的状态下改承重逻辑。离线评测集是命根子——它把"凭感觉还行"变成"可量化、可对比、可回归"。
3. **诚实不假装**:停就带原因、不烧到天花板;幂等去重不冒充精确一次;不可逆控制的后端依赖明写硬阻塞,不靠 harness 单方面兜底;失败必诚实标 `ok=False`,绝不把失败包装成成功(否则 stall 看门狗失明)。
4. **deny-by-default + fail-safe**:未知名/缺 code/超时/异常/字段缺失一律 block;解析不出绝对目标值一律拒;不可逆控制后端无 commandId/幂等前硬阻塞。
5. **接缝先行(seam-first)**:先定 Protocol 接缝、用 Fake 替换跑通骨架、再填实现。循环对治理只懂两句话(闸 allow/ask/deny、verify ok/fail);换治理不动循环;全换 Fake → 引擎确定性 → 测试绿。
6. **可逆优先(reversible-first)**:不可逆操作绝不内联进可回滚事务;必须提交一个 pending 边界之后、恢复时用幂等键执行一次。本期控制全集全可逆(暖通状态型)→ "不可逆硬阻塞"零触发对象、纯防御。
7. **渐进 + 增量**:按当下供给、为规模设计;能用 InMem 跑通就先用 InMem,不早引 Redis/PG;标准件 + 标准接口,上规模只加容量不改设计。

---

## 2. 现状盘点(2026-07-08)

### 2.1 代码

| 维度 | 现状 |
|---|---|
| 包结构 | 4 包:`agent_loop`(25 模块)/ `agent_context`(10)/ `agent_tools`(16+4 域)/ `harness_rag`(13+4) |
| 测试 | 67 测试文件 ~500 测试;三层门(单元/工厂门/live-infra 门)+ 故障注入 |
| 引擎形态 | 事务型步函数循环已落地;5 接缝(gate/control/verify/compaction/repair)接缝齐 |
| 持久化 | 双写形状(Redis+PG)已搭;B/C path + Redis TTL 已接;PG→Redis 冷加载未接(部署阶段);默认/demo 跑 InMemoryConversationStore |
| 工具治理 | v4 deny-first + 可逆性 fail-safe 落地;capability_code ⊥ is_control 正交;4 域 agent/flat 混合 |
| 控制 | propose→execute→readback 三段语义 + 挂起-恢复 + 幂等 WAL 落地;待后端 commandId 才能上不可逆 |
| 上下文/记忆 | ParkContextAssembler 5 层 + 3 级历史收敛 + 记忆 3 类 + 异步写路径;v1 上限见设计文档 §15.3 |
| 护栏 | 五道关卡骨架 + 双输入模型 + 规则/语义分层 + 显式降级;gate 已消费 principal.permissions(真权限码待 D2) |
| RAG | Milvus retriever + rerank + knowledge 工具适配;离线 Fake retriever |

### 2.2 设计文档

`harness-park-design/` 24 md 设计集(内圈/中圈/记忆/工具/控制/护栏/持久化/语音/实施计划/总纲/研究/术语约定),术语统一为"五道护栏关卡";旧称谓映射已固化。

### 2.3 语音外壳研究

`digital-human/`:深度文档 + 11 篇笔记(管线/单 WS 多路复用/事件对接/弱网状态机/协议细化)+ 协议契约(app-realtime-protocol 77KB 等)+ 腾讯 V6 收敛 20 md + 开源项目(OpenAvatarChat★ / LiveTalking / FunSpeech / FireRedChat / DH_live)。

### 2.4 Gate 状态

**G1-G6 全部已收敛(有决策记录,见 §5)**。实施不是"待决策",是"按决策落地"。剩余的是后端协调项(§8)与部署阶段工作。

详细参考:[实施计划](harness-park-design/实施计划.md)、[项目资料总览](项目资料总览.md)。

---

## 3. 落地总策略

### 3.1 三维降序:功能 → 治理 → 集成

不堆功能,按"先把治理跑通,再上功能,最后集成语音"的降序推进。治理(闸/控制/护栏/幂等/审计)是承重墙,先立;功能(四域工具/记忆/RAG)是楼板,后铺;语音外壳是装修,最后接。任何"先上功能后补治理"的顺序都是债。

### 3.2 MVP 第一、接缝先行、测试驱动、数据先行

- **MVP 第一**:每个阶段有可演示的最小闭环,不憋大招。
- **接缝先行**:Protocol 先定、Fake 先跑、实现后填。
- **测试驱动**:改行为先跑离线编排测试集;承重逻辑无回归基线不动。
- **数据先行**:工具必须能拿到后端真实数据(grounding 字典 `sycPointParamType`/`getIsCtrlTree`、设备态 `getDevicePage`、事项 `proRepairApply`/`sysAlarmRecord`/`eventReport`、权限 `/user/info`),否则工具是空壳。

### 3.3 顺序铁律

1. **后端接口先到位**(`sycPointParamType`/`getDevicePage`/`deviceCtrl`/`/user/info`),工具才有骨架——数据先行。
2. **引擎 + 工具治理先于四域工具**——治理是承重墙。
3. **真实持久化(Redis+PG)在引擎骨架稳定后、控制上线前**——控制挂起-恢复依赖可靠持久化。
4. **deviceCtrl P0 改造(commandId+幂等)在不可逆控制上线前**——硬阻塞。
5. **护栏五道关卡在控制上线前**——控制牵涉不可逆副作用。
6. **语音外壳在引擎稳定后**——外壳是薄胶水,大脑没稳没意义。
7. **可观测性贯穿,不收尾**——每阶段埋 span/metric,不最后补(retrofit 贵)。

---

## 4. 实施阶段 Phase 0-6

> Gate G1-G6 在对应 Phase 落地;决策记录见 §5。

### Phase 0:引擎骨架(事务型步循环)

**目标**:事务型步函数循环 + 5 接缝 + InMem store 跑通,全 Fake 确定性测试绿。

**内容**:
- 内圈 `run_loop`:while 推进 + 退出原因判定 + 顶部守卫(repair 序列/中断/预算/grace/本地步数)。
- 事务原子单元(gather→act→verify):`store.commit(thread_id, 新消息, boundary)` 一次原子写;回滚 `del messages[committed_len:]`。
- 5 接缝 Protocol(gate/control/verify/compaction/repair)+ Fake 实现 → 引擎确定性。
- `LoopBudget`/`BudgetTracker`(纯计数 iterations/tokens,弃墙钟)+ grace(一次性收尾)+ 子 agent(共享池+深度+权限⊆)。
- plan = todo 列表(非 DAG)+ `derive_plan` 倒扫日志 O(1)。
- 终止判定:完成=停手+正文;空响应回滚重试;stall 三件套;单工具失败≠整轮 failed;业务否 vs 基础设施失败分型。
- InMem `ConversationStore`(load/commit/latest_boundary/resolve_pending)+ codec(JSON 序列化,解码宽容未知字段,深拷贝防污染)。

**验收**:全 Fake 引擎 16/16 确定性测试绿;退出原因逻辑闭环无悬空分支;挂起-恢复(InMem)闭环。

### Phase 1:真实持久化(Redis+PG)

**目标**:控制上线前的持久化地基。Redis 热 + PG 冷,挂起 TTL,崩溃恢复,幂等账本 + 审计。

**内容**:
- Redis `ConversationStore`:messages/boundaries LIST;`commit` MULTI(RPUSH messages + RPUSH boundary);`latest_boundary` LINDEX -1(O(1));TTL 语义(awaiting EXPIRE/其他 PERSIST/refresh_on_read)。
- PG `agentloop_idem`(幂等账本 PK idem_key,三态 executed/failed/in_flight,`put_if_absent` ON CONFLICT DO NOTHING)+ `agentloop_audit`(bigserial append-only,idx_audit_thread_id)。
- B-path(纯对话 best-effort)vs C-path(控制 fail-closed):`if boundary.pending_batch: C-path else: B-path`。
- 崩溃恢复:load-to-last-boundary(torn-tail-discarded)+ budget-restore-from-snapshot + resolve_pending(no-WATCH,单 writer 收窄 TOCTOU)。
- env:`ASSISTANT_REDIS_URL`/`ASSISTANT_REDIS_CHECKPOINT_TTL_MINUTES`/`SPA_STORAGE__POSTGRES_DSN`/`AGENT_LOOP_LIVE_INFRA=1`。
- lazy-import:无 Redis/PG 跑全部 codec/settings/InMem 单测;`InMemoryConversationStore` 保持完整覆盖。

**验收**:`load()` p99<50ms / `commit()` p99<20ms / `latest_boundary()` p99<10ms;挂起 TTL 内可恢复率 100%;PG 审计写失败时纯对话边界可用性 100%(B-path)、控制边界 0%(C-path fail-closed);崩溃恢复测试(模拟迭代中途崩溃→在途丢失→从上一边界续跑)。

**诚实**:双写形状已有;控制边界 B/C path(audited_store)与 Redis TTL(EXPIRE/PERSIST)代码已接;PG→Redis 冷加载(丢 Redis 重建)未接,留部署/恢复阶段。

### Phase 2:工具治理收敛(deny-first + 控制流 + grounding)

**目标**:控制上线的治理承重墙。deny-first 闸 + capability⊥is_control + grounding 三层 + 控制三段语义。

**内容**:
- `ToolCatalog` 扁平注册表(三字段 tool/capability_code/is_control)+ `CatalogGate` deny-first(principal 没有→deny;未知名/缺 code→deny)。
- grounding 三层:LLM slots → `_resolve_value` 解析绝对目标值 → 后端 `sycPointParamType` 认证。承重信号=能否解析出绝对目标值(枚举/range→可逆;button/纯触发→不可逆)。
- 可逆性分级:只读/可逆控制(确认+读回)/不可逆控制(硬阻塞,后端无 commandId 前不上线)。
- 控制三段语义:`propose_control`(只读登记,`is_control=False`,不给句柄)→ `execute_proposal`(freeze,`is_control=True`)→ readback(`getDevicePage` 读真实状态,accepted≠effective)。
- 控制能力 `ControlCapability`:freeze(锁死动作进 awaiting 边界 + 铸造幂等键 `hash(handle+action+params)`)/resolve(查账本→put in_flight→下发→标 done/failed→pop 提案)。
- WAL 时序:查账本→F3 门栓→put in_flight→下发→update done/failed→pop。重入三态分流(done/in_flight/failed/none)。
- agent-as-tool:`make_subagent_tool` 包隔离 run_loop;结构上不可执行控制(工厂拒绝 control toolset + 运行时 control=None);2 子 agent(设施+事项)。
- 四域:facility/records(agent)+ life/knowledge(flat)+ 控制(横切 execute_proposal)。
- 组合根 `build_park_runtime` → `ParkToolRuntime`(build_tool_subsystem + login + toolset_for)。

**验收**:gate deny-first 全覆盖(无 code/未知名/无权限→deny);grounding 三层校验(可逆/不可逆/解析不出→拒);控制三段语义 + 挂起-恢复 + 幂等 WAL 端到端(可逆控制);读回对账(accepted≠effective);子 agent 结构上不可控制(类型保证非运行时纪律)。

**硬阻塞**:不可逆控制(门禁/道闸/扣费)gated 在 deviceCtrl P0 改造(§8)。本期可控全集=暖通全可逆→可暂不改;"不可逆硬阻塞"零触发对象、纯防御。

### Phase 3:上下文 + 记忆(中圈视图)

**目标**:每轮给模型看的动态视图组装 + 跨会话记忆。

**内容**:
- `ParkContextAssembler`(经 `run_loop(assembler=...)` 注入替桩):5 层组装(固定/记忆/知识/历史/任务),缓存前缀驱动 stable→front。
- 历史裁剪 3 级:`drop_answered_tool_results`(零模型,文本答案=任务交付)+ `trim_dialogue_turns`(零模型,按轮数)+ compaction(aux 模型,唯一落库 `__compaction__`)。四不丢/三不丢。压缩强框(防模型把摘要当 active 指令)。
- 任务层 `render_plan`(折叠+执行 nudge+exclude_plan_calls 配对完整性)+ 余量提示。
- 知识层 `wrap_knowledge` 强框 + 工具结果认识论框四档。
- token 预算:`estimate_tokens=char//2+1`;软阈 8000(告警+余量提示)/硬阈 ~23000(压缩)/窗 32768。
- 记忆 3 类(semantic 180d/episodic 90d/preference 365d)+ `MemoryEngine.recall/render` + `<memory-context>` 围栏 + `StreamingContextScrubber`(防伪造 fence)。
- 异步写路径:`commit` 成功后 `publish_extract` → outbox → worker → 安全扫描 → L0/L1/L2 抽取 → 生命周期 ADD/UPDATE/DELETE/NOOP → save/update/delete。content_hash 去重。写失败回 outbox 重试,0ms 影响响应。
- `Principal` frozen dataclass(身份脊柱,登录一次,不持久化,押闸不押 prompt)+ system_prompt `compose()` 纯函数(IDENTITY_VERSION=park-v14)。

**验收**:5 层组装缓存前缀稳定(同 selection→同字节,吃 vLLM prefix cache);压缩强框防"恢复旧任务";记忆围栏不被流式 chunk 戳穿;异步写失败不影响响应;principal 三消费者分流(name/role→prompt 软;token→后端硬;permissions→闸 deny 硬)。

**诚实上限**:单请求内部零缩减(丢弃跨请求、裁剪按 user 轮数单请求不触发)→ v1 兜底 context-awareness 提示;MemoryEngine recall v1 简单过滤,RRF/时间衰减/同线程降级是设计目标。

### Phase 4:护栏五道关卡

**目标**:控制上线前的安全承重墙。五道关卡 + 双输入模型 + 规则/语义分层 + 显式降级。

**内容**:
- 五道关卡:输入(防注入/紧急词/不可信)+ 意图(控制类高置信度否则 clarify)+ 准备(schema/scope/danger/manifest 签名,prepared_id TTL 60s)+ 执行(prepared_id/AuthorityClient/check_resource_state/L0-L3 确认/idempotency_key/批量冲突)+ 事实(执行类 claim 必有 tool result 支撑)。
- deny-by-default:超时/异常/字段缺失/未命中→block;规则层 100ms/语义层 300ms/执行层 1000ms 超时。
- 规则层 mandatory(超时/异常→block)vs 语义层 optional+显式降级(超时/异常→allow+degraded=1,不静默);跨层降级标记累积;降级在输出体现(免责声明/收紧阈值,意图层降级控制类置信度 +0.05)。
- 双输入模型:`bert_cn_prompt_attack_detection`(中文)+ `Llama-Prompt-Guard-2-86M`(多语言)并行取最严格;embedding 攻击样本 0.85;阈值 0.90/0.75/0.40;多轮增强方案 A(当前轮+最近 3 轮)。
- 确认分级 L0-L3(只读自动/写授权异步日志/危险弹窗/关键转人工)。
- `GuardDecision` 严格四值(allow/block/challenge/clarify,review 是异步动作非 verdict);challenge token JWT 签名 + idempotency_key + tool_call_hash(防确认前后篡改)+ expires_at 60s。
- 护栏嵌入工具/子系统(只读治理层,不写会话/不执行/不写审计,只发事件到 MQ);循环只 `awaiting_confirmation`。

**验收**:五道关卡全链路;deny-by-default 覆盖(超时/异常/缺失→block);语义层降级不静默(标记传递+输出体现);双输入模型互补(中文攻击双模型都能挡);确认闸是 ASR 控制错听最终兜底;14 条不变量全绿。

**诚实**:gate.py `CatalogGate.classify` 已消费 `principal.permissions` 做 deny-first(非 stub);真权限码供给靠后端 `/user/info` auth 端点,AI token 暂够不到——`PermissionMapper` 接缝默认直通先兜(D2,后端协调项)。

### Phase 5:语音外壳接入

**目标**:实时语音 IO 层接入大脑,端到端"说话→控制→开口"。

**内容**:
- 单 WS 多路复用:text JSON 控制/事件 + binary 音频;帧 `[4B header_length][header JSON][payload]`;per-modality asyncio Queue + 发送锁;分层 ID(session/stream/seq/transcript/msg)。
- 上行:media WS 音频 → 归一化 16k(resample_poly 抗混叠)→ fsmn-vad 切句 → 2pass dual(Paraformer partial + SenseVoice final)→ `asr.partial`/`asr.final` promotion(幂等键→run.create)。
- 下行:订阅主 WS `message.delta` → 句子聚合(标点+字数 200+首标点)→ 文本清洗(P0 去 markdown/emoji/数字规范化)→ CosyVoice2 stream → Pcm24kFramer 20ms → Opus 24k → nextPlayTime 游标排期播放。
- 打断:barge_in.detected → tts.cancelled → run.cancelling → message.cancelled → run.cancelled;双触发(服务端 VAD baseline + 前端 media.clear 可选加速);三铁律(前端 VAD 先行 200-300ms / 信令音频双路径 / LLM-TTS 可取消);误打断三层防御;服务端硬 cancel `session.task.cancel()`(全链异步 await/检查点)。
- 状态机:四态基本(IDLE/LISTENING/THINKING/SPEAKING)+ 扩展(AWAITING_FINAL/INTERRUPTED/WAITING/RECONNECTING/DEGRADED);每次转换清残留。
- 弱网:心跳 5s/2 次未 pong 重连;`control.resume`/`resume_ack`;上行缓存 2-5s 补全;jitter buffer 200-400ms RTT 自适应;四级降级 + 不可用。
- 语音↔大脑事件桥:`speech.final`→一次 run_loop(一个 final 对应一次循环,不允许多触发);`interrupt`→中断当前 run_loop;run_loop 输出流式化 + 中断语义(取消 LLM/TTS/丢弃 chunk/清工具状态/决定回滚)。

**验收**:单 WS 多路复用端到端(上行 ASR + 下行 TTS + 打断);状态机转换清残留;打断全链可取消(LLM/TTS/工具 await 检查点);弱网四级降级协商双方确认;一个 speech.final 对应一次 run_loop。

**关键**:大脑 run_loop 按轮次工作,语音是连续流;音频流不直接调 Agent,Agent 循环由语音语义事件触发。run_loop 必须可取消 + 输出流式化。

### Phase 6:可观测性 + 评测闭环

**目标**:三支柱 × 三层 + 在线 eval + badcase 回流 + SLO 告警。

**内容**:
- 标准拓扑:应用(OpenLLMetry 自动埋 LLM + 手埋 harness span)→ OTel Collector(适配中枢)→ Langfuse(LLM trace+在线 eval)/ Tempo / Prometheus→Grafana+Alertmanager / Loki。
- **★ spanmetrics connector 在采样前从 100% 流量派生 RED 指标**(否则失真)。
- 埋点:LLM 调用 `gen_ai.*` 自动;harness 自定义 span(run_loop/iteration/tool/gate/RAG/control)手埋;命名约定钉 `OTEL_SEMCONV_STABILITY_OPT_IN=gen_ai_latest_experimental`。
- 指标四类:延迟(TTFT/TPOT/端到端/Goodput)+ 用量成本(token/请求/成本)+ 错误可用(错误率/超时/降级/可用性)+ 质量安全(在线 faithfulness/任务完成/工具选择/漂移/越权/注入)+ 资源(GPU 占用/并发分档)。
- SLO:可用性 99.9%(月预算 ~43min)/ TTFT p95 ≤2s(简单)≤3s(复杂)/ 越权=0(硬)。multi-window burn-rate(1h 14.4×页面 / 6h 6× / 3d 1×工单)。quality-aware 告警(eval 分跌/漂移/越权≠0/降级骤升)。
- PII 脱敏双层(instrumentation 不记敏感 + Collector processor 兜底,routing/export 前)。
- badcase 回流:100% 启发式 + 5-15% 采样 + 100% 错误 trace 跑本地 LLM-judge;漂移检测(embedding+分布);trace→case 转换器(自建,别依赖 SaaS);判官校准(5-10% 人审+Cohen's kappa 地板 0.6,kappa 跌告警)。
- 审计 vs 监测互补:`PgAuditLog` 合规留痕(写边界、模型不可写)与 OTel trace 并行,挂同一边界、各走各 sink,别用 trace 替审计。

**验收**:三支柱齐全;spanmetrics 采样前派生;quality-aware 告警能抓 APM 抓不到的静默失败;badcase 回流闭环(生产 trace→回归用例→CI 重测);判官 kappa≥0.6。

---

## 5. Gate 决策记录 G1-G6(全部已收敛)

> 所有 Gate 已决策,实施按决策落地。摘自 [实施计划](harness-park-design/实施计划.md)。

### G1:循环模型——递归 ReAct + 原生 tool-calling(已收敛)

**决策**:递归 ReAct(模型自己决定下一步),非状态机编排。原生 tool-calling(provider 原生函数调用),非文本协议解析。

**理由**:园区场景工具组合空间大,状态机不够灵活;原生 tool-calling 比 prompt 约定更可靠。循环本体只管"怎么推进",政策全在接缝外。

### G2:闸的位置——工具/护栏子系统,循环只 awaiting(已收敛)

**决策**:闸(ask/allow/deny)在工具/护栏子系统;内圈循环只负责 `awaiting_confirmation` 挂起-恢复,带不透明 pending 句柄。

**理由**:循环不 own 治理(否则换治理要改循环);护栏是只读治理层嵌入工具/子系统。循环对治理只懂两句话。

### G3:控制流的执行者——恢复时引擎执行冻结动作,模型不重发(已收敛)

**决策**:挂起时把控制 tool_call 填 `[pending_confirmation]` 占位 + boundary 带 pending_batch;恢复由引擎直接执行冻结动作,模型不重新插手。

**理由**:否则模型可能改主意/幻觉成别的动作,"你确认的"≠"它做的"。这是事务正确性与安全的承重设计。

### G4:幂等键铸造时机——挂起时铸造(已收敛)

**决策**:幂等键 `idem_key = hash(handle+action+params)`(确定性派生,非 uuid4)在 freeze 时铸造进 awaiting 边界。**代码已遵**:生产 `ProposalControlCapability` 用 `sha256(...)[:32]`;仅测试路径 `PgControlCapability` 漂移用 uuid4(见 §8.2 R1)。

**理由**:重启/重入算出同一个 key 才能在账本查到上次记录;随机 key 让 WAL 失效。

### G5:预算墙钟——弃墙钟,纯计数(已收敛)

**决策**:预算 iterations/tokens 纯计数,零时间。挂起期间无模型调用=自然不消耗。`grace_used` 持久化。

**理由**:挂起期间预算被墙钟烧是硬骨头;纯计数解之。代码 `BudgetTracker` 已纯计数(`_iters/_tokens/_grace_used`,无墙钟字段),G5 已满足。(旧记曾提 `deadline_s` 字段冲突,经核代码无此字段,该项作废,见 §8.2 R7。)

### G6:plan 形态——todo 列表(非 DAG),条件 DAG 移除(已收敛)

**决策**:plan = 带 status 的 todo 列表(非 DAG)。并行=一轮多 tool_call、串行=ReAct 顺序、重试=循环动态,都不需要 DAG。

**理由**:DAG 复杂度高、收益低;ReAct 循环已足够表达。条件 DAG 在收敛中移除。

---

## 6. 评测方案

### 6.1 评测优先(eval-first)

先有测试集再写功能。承重逻辑改动先跑离线编排测试集,绿了才合。不在无回归基线状态下改承重逻辑。

### 6.2 三层评测

| 层 | 内容 | 工具 | 频率 |
|---|---|---|---|
| 离线编排测试集 | 园区场景用例(查设备/控制/事项综合/生活/知识),含 gold + 断言 | 自建编排测试集(数字人腾讯 V6 收敛已有) | 每次 PR / CI |
| 在线 eval | faithfulness / 任务完成 / 工具选择率 / 漂移 | Langfuse 在线 eval | 持续 |
| badcase 回流 | 启发式 100% + 采样 5-15% + 错误 100% LLM-judge | trace→case 转换器 + 本地 qwen 判官 | 持续 |

### 6.3 离线编排测试集

- 用例覆盖四域 + 控制确认 + 打断 + 弱网 + 边界(空输入/超长/注入/越权)。
- 每用例:输入 + gold 输出 + 断言(工具调用序列/是否确认/读回/护栏 verdict/plan 收敛)。
- 断言分层:硬断言(必须满足,如越权=block)+ 软断言(统计,如工具选择率)。
- 不依赖 SaaS:trace→case 转换器自建,生产 trace 一键转回归用例(补 gold/断言/脱敏)。

### 6.4 在线 eval 指标

| 指标 | 含义 | 阈值(借鉴附录A,非投标对标) |
|---|---|---|
| faithfulness | 回答是否忠于工具结果 | ≥90% |
| 任务完成 | 是否完成用户意图 | ≥85% |
| 工具选择率 | 选对工具的比例 | ≥95% |
| 幻觉率 | 无支撑的执行类声称 | ≤5% |
| 越权次数 | 闸 deny 漏过 | =0(硬) |
| 注入命中率 | 输入护栏挡住 | ≥90% |
| 拒答率 | 该拒的拒了 | ≥90% |
| 安全幻觉 | 控制类安全幻觉 | ≤0.1% |
| 好评率 | 用户反馈 | ≥80% |

### 6.5 LLM-judge 校准

对在线 LLM-judge 分:地板 5-10% 人审抽检 + Cohen's kappa(地板 0.6);kappa 跌即告警。判官本地 qwen(等保,数据不出域)。

### 6.6 SLO

- 可用性 99.9%(月错误预算 ~43min)。
- TTFT p95 ≤2s(简单)/≤3s(复杂)。
- 越权=0(硬)。
- multi-window burn-rate(1h 14.4×页面 / 6h 6×页面工单 / 3d 1×工单)。
- 降级可用性 100%、切换≤3s。

详细参考:[监测层设计_可观测标配栈](digital-human/腾讯V6项目收敛/监测层设计_可观测标配栈.md)、[AI服务可观测性设计规范_V1.1](digital-human/腾讯V6项目收敛/AI服务可观测性设计规范_V1.1.md)。

---

## 7. 降级矩阵(各子系统挂了怎么办)

| 子系统 | 故障 | 降级行为 | 性质 |
|---|---|---|---|
| 内圈循环 | 模型超时/异常 | 超时零重试(可能已副作用);瞬时异常只读重试;连续失败熔断→failed | fail-safe |
| | 预算耗尽 | grace 一次性收尾(不执行工具)→budget_exhausted 诚实标 | 诚实 |
| | stall 三件套 | 原地踏步/光说不做/瞎忙活→连续 3 次回滚 failed | 诚实 |
| 上下文 | 超硬阈 | compaction aux 摘要(唯一落库) | 收敛 |
| | 压缩模型失败 | 降级 trim(零模型);trim 不够→drop_answered | 降级 |
| | 单请求内部 | 零缩减(兜底 context-awareness 提示让模型早收尾) | v1 上限 |
| 记忆 | 读失败/超时 | 空集,对话继续,ops 告警 | 不阻塞 |
| | 写失败 | 事件回 outbox 重试,0ms 影响响应 | 不阻塞 |
| | 向量索引不可用 | 退化为关键词+时间 | 降级 |
| | 全部不可用 | 空集 | 降级 |
| | 安全扫描失败 | fail-closed 拒写 | fail-safe |
| 工具治理 | 解析不出绝对目标值 | 拒(不上线) | fail-safe |
| | 不可逆控制后端无 commandId | 硬阻塞(不上线) | fail-safe |
| | 权限码缺失/未知名 | deny | deny-first |
| 控制 | 幂等账本 done | 返缓存,绝不重发 | 真幂等 |
| | in_flight(不可逆) | 绝不重发(返状态未知) | at-most-once |
| | in_flight(可逆) | 可幂等重试 | at-most-once |
| | failed | 重发(真重试) | 重试 |
| | 读回 accepted≠effective | outcome 标未生效,不假装成功 | 诚实 |
| 护栏 | 规则层超时/异常 | block | fail-safe |
| | 语义层超时/异常 | allow+degraded=1(不静默,标记传递+输出体现) | 显式降级 |
| | 意图层降级 | 控制类置信度 +0.05(收紧非放宽) | 收紧 |
| 持久化 | PG 审计写失败(纯对话) | B-path warning,不阻断 | best-effort |
| | PG 审计写失败(控制) | C-path fail-closed,上抛异常 | fail-closed |
| | Redis 崩溃 | 从 PG audit 重建热态;在途迭代丢失,从上一边界续跑 | 恢复 |
| | resolve_pending TOCTOU | 单 writer 收窄(非零);多 writer 需 Redis Stream/锁 | 已知风险 |
| RAG | Milvus 不可用 | 退化为关键词+时间;全部不可用→未命中/不可用,绝不臆造 | 降级 |
| | 无命中 | `ok=True, content="[未检索到相关知识]"` | 诚实 |
| 语音外壳 | RTT 200-500ms 丢包 1-5% | 轻度降级(增 jitter/TTS chunk 200ms/减 viseme) | 协商 |
| | RTT>500ms 丢包 5-10% | 中度降级(关唇形/TTS 整句/ASR 批量) | 协商 |
| | 频繁断线丢包>10% | 重度降级(只文本,关语音,提示切文字) | 协商 |
| | 重连失败 | 不可用(结束会话) | 终止 |
| | 打断误判 | 三层防御(本地 VAD 200-300ms + 服务端权威 + voice_policy safety_phrases) | 防误打断 |
| 可观测 | Collector 故障 | 应用直连 Langfuse(单 Collector)/ 降级(应用零改,接缝保证) | 接缝 |
| | spanmetrics 失真 | 必须采样前派生(否则指标失真) | 铁律 |

---

## 8. 硬依赖与风险登记

### 8.1 后端协调项(阻塞上线)

| # | 依赖 | 谁负责 | 阻塞什么 | 现状 | 缓解 |
|---|---|---|---|---|---|
| D1 | 后端 deviceCtrl 加 `commandId`+幂等去重(P0)+ `commandStatus` 查询(P1)+ `expectedVersion` 乐观锁(P2) | 后端 | 不可逆控制上线 | 未做 | 本期可控全集全可逆(暖通)→可暂不改;门禁/道闸/扣费纳入才必须改;雏形用桩控制能力先跑通挂起/恢复/幂等接口 |
| D2 | `/user/info` auth 端点(permissions/apiPermissions/devicePermission/roleKey) | 后端 | 真权限闸 deny | AI token 够不到 | `PermissionMapper` 接缝默认直通先兜;待后端开 auth 端点给样例 |
| D3 | `getRoleAuthDevice/{roleId}` admin 返空 | 后端 | 设备级权限校验 | 待 auth 端点 | 同 D2 |
| D4 | `dataScope` 语义确认 | 后端 | 数据级权限过滤确认 | 需真机确认 | 留后端 token 过滤,harness 不碰 |
| D5 | org 策略规则口径 | 业务方 | PrincipalResolver 收紧 | 留配置接口 | 先留默认 |

### 8.2 设计/实现缺口(非外部阻塞,需内部收敛)

> 2026-07-08 逐条对照源码核实(见 §8.4 核实笔记)。曾误记为"缺口"但代码已实现的项,保留为「作废」行以留审计痕迹,不静默删除。

| # | 现状(已核代码) | 影响 | 处置 |
|---|---|---|---|
| R1 | 生产 `ProposalControlCapability` 幂等键**已用**确定性 `sha256(handle+action+params)[:32]`(G4 已满足);漂移在测试路径 `PgControlCapability`(uuid4 + echo、无 readback/F3/in_flight 三态) | PG e2e 校不到真 WAL 语义 | 将 `PgControlCapability` 对齐生产语义或退场;勿让 e2e 走简化桩 |
| R2 | `MemoryEntry` 缺 content_hash/updated_at/source_boundary_seq | 去重/溯源不完整 | 补字段 |
| R3 | gate.py `CatalogGate.classify` **已消费** `ctx.principal.permissions` 做 deny-first(gate.py:37-39,非 stub);真权限码供给靠后端 `/user/info` auth 端点(D2) | 真权限码未到位时接缝默认直通 | 待 D2 后端开 auth 端点给权限码 |
| R4 | 知识 `wrap_knowledge(content, source="")` source 槽就位,通道未接 | 出处不显 | 接 source 通道 |
| R5 | 子 agent 四种结果(completed/budget_exhausted/failed/interrupted)经 dispatch「业务层 ok=False 仍 executed」规则(dispatch.py:109-114),**不**计入父 `max_tool_failures`(只回喂 `[error]` 让模型调整);分型已就位 | 无(原记「污染」不成立) | 作废;仅留意子 handler 抛未映射异常/超时才走 failed |
| R6 | 空文本已回滚重试、连续耗尽(`MAX_EMPTY=2`)才 failed(loop.py:298-303) | 无(原记「返回 completed」不成立) | 作废 |
| R7 | `BudgetTracker` 已纯计数(`_iters/_tokens/_grace_used`,无 `deadline_s` 字段,G5 已满足) | 无(原记字段不存在) | 作废 |
| R8 | 控制边界 B/C path(audited_store.py:78-81)✓、Redis TTL(EXPIRE/PERSIST/refresh_on_read,redis_store.py:100-121)✓ 已接;**PG→Redis 冷加载(丢 Redis 后从审计账本重建热存)未接** | 丢 Redis 后无法从审计账本重建热存 | 部署/恢复阶段补冷加载 |
| R9 | 审计存储落点 schema 未定(用引擎 PG 账本还是单独治理事件表) | 治理事件 schema | 待定 |

### 8.3 风险登记

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 后端 commandId 改造滞后 | 中 | 不可逆控制无法上线 | 本期全可逆可暂不改;门禁/道闸/扣费纳入才阻塞;雏形桩控制能力先行 |
| `/user/info` auth 端点不开 | 中 | 真权限码不到位,deny 接缝默认直通 | gate 已消费 `principal.permissions`(非 stub);接缝默认直通先兜,越权靠"不进候选集+后端过滤"兜底 |
| OTel GenAI 语义约定未冻结 | 高 | 埋点字段小变 | 钉版本,预期小变,应用零改(Collector 适配) |
| 平台监测开销 ~15% | 中 | 性能 | 单一基准非普适常数,场景依赖;埋点深度可调 |
| resolve_pending TOCTOU(多 writer) | 低 | 并发 resolve 竞态 | 单 writer 收窄;多 writer 引 Redis Stream/锁 |
| 语音打断工具不可取消 | 中 | 查到一半改主意停不下 | 工具埋 await/检查点(园区会发生) |
| LLM-judge 漂移 | 中 | badcase 标注不准 | 5-10% 人审 + Cohen's kappa 地板 0.6 + kappa 跌告警 |
| 压缩后模型把摘要当 active 指令 | 中 | 恢复/重答旧任务 | 压缩强框(已抄,真失败模式) |
| 记忆 fence 被流式 chunk 戳穿 | 低 | 召回内容被当新输入 | StreamingContextScrubber 跨 chunk 清理 |

### 8.4 核实笔记(2026-07-08 逐条对照源码)

| # | 核实结论 | 证据(file:line) |
|---|---|---|
| R1 | 生产用确定性 hash,uuid4 仅在测试桩 | `proposal_control.py:45-49`(sha256);`pg_store.py:190`(uuid4,测试路径);`control.py:34`(FakeControlCapability 桩) |
| R3 | gate 真消费 principal.permissions | `gate.py:37-39` |
| R5 | 子 agent 失败经 executed 不计 failures | `subagent.py:83-107`(分型);`dispatch.py:109-114`(业务 ok=False 仍 executed) |
| R6 | 空文本已回滚重试+failed | `loop.py:298-303`(`MAX_EMPTY=2`) |
| R7 | 无 deadline_s,纯计数 | `budget.py:8-10`;`config.py`(LoopBudget 无 deadline) |
| R8 | B/C path + TTL 已接,冷加载未接 | `audited_store.py:78-81`(C/B);`redis_store.py:100-121`(TTL);冷加载路径未发现 |
| 附 | 子 agent=1(仅 facility),records 扁平;顶层工具=8 | `composition.py`(TOP_TOOLS=8);`domains/records.py:42`(`make_record_query_tool`,注释"扁平在顶层") |
| 附 | `tokens.py` 公式未直核(读权限受限),单调性已由单测确认 | `tests/test_agent_context_history.py:43` |

---

## 9. 测试策略

### 9.1 三层门

| 门 | 范围 | 触发 | 性质 |
|---|---|---|---|
| 单元门 | 全 Fake,引擎 + 各子系统纯逻辑 | 每次 PR | 确定性,快 |
| 工厂门 | `build_park_runtime` 组合根,验证工具注册/权限加载/工具集可见性 | 每次 PR | 组装正确性 |
| live-infra 门 | 真 Redis+PG+LLM(vLLM)+Milvus,gated `AGENT_LOOP_LIVE_INFRA=1` | 手动/夜间 | 真集成 |

### 9.2 故障注入

- 模型超时/异常/空响应/stall 三件套。
- 工具业务否 vs 基础设施失败分型。
- 持久化:迭代中途崩溃→在途丢失→从上一边界续跑;PG 审计失败 B-path vs C-path;Redis 崩溃→PG 重建。
- 控制:幂等账本 done/in_flight/failed/none 四态分流;读回 accepted≠effective;重复 resume/重连/崩溃后重发同键。
- 护栏:规则层超时→block;语义层超时→allow+degraded;双输入模型互补。
- 语音:打断(barge_in→全链 cancel);弱网四级降级;误打断三层防御。

### 9.3 语音契约测试

- 单 WS 多路复用帧协议(text JSON + binary `[4B header_length][header JSON][payload]`)。
- 分层 ID 单调性(session/stream/seq/transcript/msg)。
- 状态机转换清残留(每次转换清 cache/buffer/队列)。
- 一个 speech.final 对应一次 run_loop(不允许多触发)。
- 打断全链可取消(LLM/TTS/工具 await 检查点)。

### 9.4 评测回归

- 离线编排测试集:每次 PR 跑,硬断言必绿,软断言统计。
- badcase 回流:生产 trace→回归用例→CI 重测。
- 在线 eval:faithfulness/任务完成/漂移持续监控,跌破阈值告警。

详细参考:[实施计划](harness-park-design/实施计划.md)、各分册实施计划。

---

## 10. 部署与配置

### 10.1 环境变量

| 变量 | 说明 | 默认 |
|---|---|---|
| `ASSISTANT_REDIS_URL` | Redis URL | `redis://localhost:6379/0` |
| `ASSISTANT_REDIS_CHECKPOINT_TTL_MINUTES` | 挂起 TTL(分钟) | 60 |
| `ASSISTANT_REDIS_CHECKPOINT_REFRESH_ON_READ` | load 时刷新 TTL | true |
| `SPA_STORAGE__POSTGRES_DSN` | Postgres DSN | `postgresql://postgres:postgres@localhost:5432/smart_park` |
| `ASSISTANT_LLM_BASE_URL` | LLM base URL | `localhost:6008/v1` |
| `ASSISTANT_LLM_API_KEY` | LLM API key | `local-vllm-llm` |
| `ASSISTANT_LLM_MODEL` | LLM model | `chat` |
| `AGENT_LOOP_LIVE_INFRA` | live-infra 门开关 | off |
| `OTEL_SEMCONV_STABILITY_OPT_IN` | OTel GenAI 语义约定版本 | `gen_ai_latest_experimental` |

### 10.2 拓扑

```
客户端(微信小程序/Web/数字人)
  │  WS(主 realtime + media)
  ▼
[语音外壳 media WS] + [主 WS /app/realtime/ws]
  │  speech.final / message.delta
  ▼
[大脑 harness(内圈+中圈+工具+RAG)]
  │  tool calls / control
  ▼
[后端园区平台(权威:权限/数据/设备下发)]
  │
  ├─ [Redis(热:messages/boundaries)]
  ├─ [PostgreSQL(冷:idem ledger + audit)]
  ├─ [Milvus(RAG 向量)]
  └─ [vLLM(LLM qwen)]
  
可观测:应用 → OTel Collector → Langfuse/Tempo/Prometheus/Grafana/Loki
```

### 10.3 分级上线(staged rollout)

1. **InMem 演示**:全 Fake + InMem store,本地跑通端到端(Phase 0-2 雏形)。
2. **单实例 + 真后端**:live-infra 门开,真 Redis+PG+LLM+Milvus,单实例(Phase 1-4)。
3. **语音接入**:单实例 + 语音外壳(Phase 5)。
4. **可观测 + 评测闭环**:接 Collector + Langfuse + Prometheus(Phase 6)。
5. **多实例 + 上规模**:Collector 转 agent→gateway(≥2 实例+LB 按 traceID 一致性路由),tail-sampling 仅 gateway;按并发分档扩容。

**不上 SQLite**(单写者 + 多进程会损坏);**不早引 Redis/PG**(能用 InMem 跑通就先用)。

---

## 11. 腾讯 V6 对齐

### 11.1 对齐原则

- **取其骨架,非 1:1 复制**:V6 是参考蓝本,不是必须照抄的规范。取其架构骨架与标准件选择,本项目按园区场景与微信小程序约束裁剪。
- **传输无关 + 分层可选**:双 WS 基线(主 WS + media WS)强制必做;重特性(WebRTC 传输、后端 AEC、cam++ 说话人过滤)全做成可选层、留开关。
- **可观测标配栈照搬**:V6 的"标配优先 + 标准接口 + 三支柱 × 三层 + Collector 居中 + spanmetrics 采样前派生"是成熟方案,直接采用。

### 11.2 采纳 vs 不采纳

| V6 内容 | 本项目 | 理由 |
|---|---|---|
| 标配可观测栈(OTel+Langfuse+Prometheus+Grafana+Tempo+Loki) | ✓ 采纳 | 成熟标准件 |
| Collector 居中 + spanmetrics 采样前 | ✓ 采纳 | 适配中枢,指标不失真 |
| 编排测试集 | ✓ 采纳(自建) | eval-first 回归基线 |
| 双 WS 基线(主+media) | ✓ 采纳 | 微信小程序公约数 |
| FunASR 2pass + CosyVoice 原生 | ✓ 采纳 | 原生输出与协议字段 1:1 |
| WebRTC 传输 | ✗ 不采纳(留开关) | 微信小程序做不了标准 WebRTC |
| Pipecat/LiveKit 框架 | ✗ 不采纳 | 编排在协议侧,框架被掏空 |
| 后端 AEC / cam++ 说话人过滤 | 留开关 | 重特性,可选层 |

### 11.3 参考定位

`digital-human/腾讯V6项目收敛/` 的 20 md + 编排测试集 + 监测层设计 + 可观测规范是**参考蓝本**,不是本项目规范。本项目设计以 `harness-park-design/` + `digital-human/笔记/` 为准,V6 收敛材料作深度参考。附录 A(V6 提供的指标阈值表)是"借鉴参考"非"投标对标"。

---

## 12. 里程碑与验收(Definition of Done)

### M0:引擎骨架(Phase 0)
- [ ] 全 Fake 引擎 16/16 确定性测试绿
- [ ] 退出原因逻辑闭环无悬空分支
- [ ] 挂起-恢复(InMem)闭环
- [ ] 5 接缝 Protocol 齐 + Fake 实现

### M1:真实持久化(Phase 1)
- [ ] Redis+PG 双写;`load` p99<50ms / `commit` p99<20ms / `latest_boundary` p99<10ms
- [ ] 挂起 TTL 内可恢复率 100%
- [ ] B-path vs C-path(纯对话 best-effort / 控制 fail-closed)
- [ ] 崩溃恢复测试绿(迭代中途崩溃→从上一边界续跑)
- [ ] PG→Redis 冷加载属部署阶段(B/C path + TTL 已接);形状接通即可

### M2:工具治理(Phase 2)
- [ ] gate deny-first 全覆盖
- [ ] grounding 三层校验(可逆/不可逆/解析不出→拒)
- [ ] 控制三段语义 + 挂起-恢复 + 幂等 WAL 端到端(可逆控制)
- [ ] 读回对账(accepted≠effective)
- [ ] 子 agent 结构上不可控制(类型保证)
- [ ] 四域(facility/records/life/knowledge)+ 控制横切
- [ ] 不可逆控制 gated 在 deviceCtrl P0(D1)

### M3:上下文+记忆(Phase 3)
- [ ] 5 层组装缓存前缀稳定(同 selection→同字节)
- [ ] 历史裁剪 3 级 + 压缩强框
- [ ] 记忆 3 类 + 围栏 + StreamingContextScrubber
- [ ] 异步写路径(写失败不影响响应)
- [ ] Principal 三消费者分流

### M4:护栏五道关卡(Phase 4)
- [ ] 五道关卡全链路
- [ ] deny-by-default 覆盖
- [ ] 语义层显式降级不静默
- [ ] 双输入模型互补
- [ ] 确认闸是 ASR 错听兜底
- [ ] 14 条不变量绿
- [ ] principal 闸 deny 待 D2(接缝先兜)

### M5:语音外壳(Phase 5)
- [ ] 单 WS 多路复用端到端
- [ ] 状态机转换清残留
- [ ] 打断全链可取消
- [ ] 弱网四级降级协商
- [ ] 一个 speech.final 对应一次 run_loop

### M6:可观测+评测(Phase 6)
- [ ] 三支柱齐全
- [ ] spanmetrics 采样前派生
- [ ] quality-aware 告警
- [ ] badcase 回流闭环
- [ ] 判官 kappa≥0.6
- [ ] SLO 99.9% + multi-window burn-rate

### 上线门槛(承重)
- [ ] **不可逆控制上线 = deviceCtrl P0 改造完成(D1)**——硬阻塞,harness 侧读回收窄非保证
- [ ] **真权限闸 deny = `/user/info` auth 端点(D2)**——否则 stub 兜底
- [ ] **控制上线前 = 护栏五道关卡(M4)**——控制牵涉不可逆副作用
- [ ] **持久化可靠 = Redis+PG(M1)**——控制挂起-恢复依赖

---

## 附录 A:文档导航

### A.1 配套文档

| 文档 | 用途 |
|---|---|
| [项目设计文档](项目设计文档.md) | 建什么(架构/子系统/接口/不变量) |
| [项目资料总览](项目资料总览.md) | 资料盘点(代码/设计/研究/Word/日志) |
| [实施计划](harness-park-design/实施计划.md) | G1-G6 Gate 决策原始记录 |
| [术语表与文档联动约定](harness-park-design/术语表与文档联动约定.md) | 术语锚点 |

### A.2 各 Phase 实施计划

| Phase | 实施计划文档 |
|---|---|
| 0-1 引擎 | [内圈引擎实施计划-agent_loop](harness-park-design/内圈引擎实施计划-agent_loop.md) / [内圈循环-接缝补全-实施计划](harness-park-design/内圈循环-接缝补全-实施计划.md) / [内圈循环-真实持久化-实施计划](harness-park-design/内圈循环-真实持久化-实施计划.md) |
| 2 工具/控制 | [工具管理子系统-设计 §实施](harness-park-design/工具管理子系统-设计.md) / [devicectrl-接口改造建议](harness-park-design/devicectrl-接口改造建议.md) |
| 3 上下文/记忆 | [上下文管理子系统-设计](harness-park-design/上下文管理子系统-设计.md) / [memory-subsystem-design](harness-park-design/memory-subsystem-design.md) |
| 4 护栏 | [harness护栏安全子系统设计说明](harness-park-design/harness护栏安全子系统设计说明.md) |
| 5 语音 | [语音交互子系统-设计](harness-park-design/语音交互子系统-设计.md) / [语音事件与harness-run_loop对接](digital-human/笔记/语音事件与harness-run_loop对接.md) |
| 6 可观测 | [监测层设计_可观测标配栈](digital-human/腾讯V6项目收敛/监测层设计_可观测标配栈.md) / [AI服务可观测性设计规范_V1.1](digital-human/腾讯V6项目收敛/AI服务可观测性设计规范_V1.1.md) |

---

## 附录 B:版本与变更

- **v1.0 (2026-07-08)**:首版权威综合。基于 `harness-park-design/实施计划.md` Gate 决策 + 各分册实施计划 + 代码现状 + 评测/降级策略凝成;G1-G6 全部已收敛;硬依赖(D1-D5)与风险登记(R1-R9)明写;MVP 路线 Phase 0-6 + 里程碑 M0-M6。不重复设计的"为什么"——那在《项目设计文档》。
